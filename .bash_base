# To add this to the config, run:
# echo "[[ -f ~/.bash_base ]] && . ~/.bash_base" >> ~/.bashrc

alias v='vim'
alias vd='vimdiff'
alias s='source ~/.bashrc'
alias g='git'

[[ -f ~/.bash_local ]] && . ~/.bash_local
